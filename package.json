{
  "name": "gmtk-2019-jam-entry",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "autoprefixer": "^9.6.0",
    "jonikorpi-base-files": "^14.0.0",
    "parcel-bundler": "^1.12.3",
    "preact": "^10.0.0-rc.0"
  },
  "scripts": {
    "start": "parcel --open --port 3000 index.html",
    "build": "rm -r ./dist && parcel build --experimental-scope-hoisting --out-file index.html",
    "deploy": "now ./dist --local-config='../now.json'"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
